import React from 'react';
import BoardConfigForm from '../components/BoardConfigForm';

const BoardConfigContainer = () => {
  return <BoardConfigForm />;
};

export default React.memo(BoardConfigContainer);
